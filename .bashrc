#!/bin/bash
# Project-specific Gradle alias
# Source this file in your shell to use the 'gradle' command with the wrapper

# Define an alias for the 'gradle' command that uses the wrapper instead
alias gradle='./gradlew'

echo "Tailwind Smart Plugin: 'gradle' command has been aliased to use './gradlew' for this session"
